{% extends "layout.twig" %} {% block content %}


<!-- Block Question  -->

<div class="text-center">
	<h1>{{ quiz.question.title }}</h1>
</div>

<!-- Block Answers -->
<hr />

<form id="quizform" method="post">
    <input type="hidden" name="selection" id="selection" value="" />
	<div class="row">
		{% for answer in quiz.answers %}
		<div class="col-xs-4" >
		  <a data-val="{{ answer.image }}" href="#" class="selection thumbnail">
			<img  src="/themes/starwars/images/{{ answer.image }}" style="max-height:100px" class="img-rounded" alt="{{ answer.title }}">
			<div class="text-center">
				<strong>{{ answer.title }}</strong>{{ answer.text }}
			</div>
		  </a>
		</div>
		{% endfor %}
	</div>
</form>

<hr />

{% endblock %}
