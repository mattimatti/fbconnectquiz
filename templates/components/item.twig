<div class="col-xs-6 col-md-4 col-lg-4 box">
	<a data-val="{{ answer.id }}" href="#" class="selection thumbnail">
	<img src="/themes/starwars/images/{{ answer.image }}" alt="{{ answer.title }}">
		<div class="text-center">
			<h4>{{ answer.title }}</h4>
{# 			<p>{{ answer.message }}</p>#}
		</div>
	</a>
</div>